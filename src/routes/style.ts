import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  tablabel: {
    fontSize: 13,
    fontFamily: 'Roboto-Regular',
    lineHeight: 20,
    color: '#28AF51',
  },
});
